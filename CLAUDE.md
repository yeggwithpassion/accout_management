## Agent skills

### 需求跟踪器

需求以 markdown 文件形式存放在 `.scratch/<feature-slug>/` 下。详见 `docs/agents/issue-tracker.md`。

### Triage 标签

使用中文标签：`待评估` / `待补充` / `AI可接` / `需人工` / `不予处理`。详见 `docs/agents/triage-labels.md`。

### 领域文档

多上下文布局——根目录 `CONTEXT-MAP.md` 指向各子系统的 `CONTEXT.md`。详见 `docs/agents/domain.md`。
