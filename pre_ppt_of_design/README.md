# 设计报告 Pre PPT

入口文件：`index.html`

使用方式：

- 直接用浏览器打开 `index.html`。
- 按 `F` 全屏播放。
- 左右方向键翻页。

说明：

- 本演示使用本地安装的 reveal.js，不依赖 CDN。
- 页面中的图均从 `系统设计报告_资金证券管理系统_合并版_表格统一格式.docx` 中提取，存放在 `assets/docx_images/`。
- 全部内容共 15 页。
