# 账户管理子系统——安全测试报告

测试范围：账户业务子系统后端（Spring Boot + MySQL，源码位于 `src/`）

执行日期：2026-06-21

测试方式：静态代码审计 + 基于 H2 + MockMvc 的动态接口安全测试

测试结果：**67 个测试通过 / 0 失败 / 8 个跳过（真实 MySQL 集成）**，其中 **17 个为本次新增的安全测试**（`src/test/java/account/integration/SecurityHardeningTest.java`）。

---

## 1. 测试目标

本次测试针对 **OWASP Top 10** 中与本系统业务面相关的风险展开：

- A01 鉴权失效 / 横向越权（IDOR）
- A02 密码学失效（密码哈希、令牌生成与传输）
- A03 注入（SQL 注入、XSS）
- A04 设计缺陷（无认证的回调接口、缺失 RBAC）
- A05 安全配置错误（默认凭证、异常处理回显）
- A07 身份认证失败（用户枚举、令牌失效）
- A09 安全日志记录与监控（令牌通过 query 泄露到 log）

## 2. 测试方法

| 维度 | 工具 / 方式 |
|---|---|
| 静态审计 | 人工通读 `controller/`、`service/`、`dao/`、`exception/`、`config/`、`integration/` |
| 动态测试 | JUnit 5 + Spring `MockMvc` + H2 (MODE=MySQL) 内存库；不依赖任何外网服务 |
| 测试触发 | `mvn test -Dtest=SecurityHardeningTest` |

测试套件分为 **防护型测试**（预期通过，证明系统已挡住该类攻击）与 **漏洞固化测试**（预期通过，把当前已知弱点用断言记录下来，修复后断言会失败，提醒开发感知）。

## 3. 修复的预存编译问题

在执行测试前，发现仓库存在一个上游遗漏：`StaffService` 接口引用了 `ChangeStaffPasswordRequest`，但缺少 import；`StaffServiceImpl` 也未实现该方法。已做最小修复：

- `src/main/java/account/service/api/StaffService.java`：补 import
- `src/main/java/account/service/StaffServiceImpl.java`：补全 `changePassword`（查 staff → 校验旧密码 → 写新哈希 → 落 `operation_log`）

修复后基线 `mvn test` 50 个原有测试全部通过。

## 4. 防护型测试（11 项，全部通过）

| 编号 | 测试方法 | 攻击向量 | 断言要点 | 结果 |
|---|---|---|---|---|
| S-1 | `sqlInjection_loginPayloadIsParameterized` | 登录字段塞 `' OR '1'='1` / `UNION SELECT password_hash` / `DROP TABLE staff` / 内联注释 4 种载荷 | 全部命中 ERR_010 业务错误，且 `staff` 表未被破坏，合法账号仍能登录 | ✅ |
| S-2 | `sqlInjection_queryParameterIsParameterized` | `GET /api/external/fund/snapshot?fund_acc_no=FA...' OR '1'='1` | 注入载荷被参数化绑定，命中业务错误，不会回退到他人账户 | ✅ |
| S-3 | `xss_responseContentTypeAndJsonEscape` | 用 `<script>alert(1)</script>` 作姓名开户 | 响应 `Content-Type` 严格 `application/json`，不会被浏览器当 HTML 渲染执行 | ✅ |
| S-4 | `idor_clientCannotAccessOtherFundSnapshot` | A 持有 `auth_token`，请求 `fund_acc_no=B` | 返回 ERR_018（`InMemoryClientAuthTokenService#requireFundAccess` 强校验 token 绑定的 fundAccNo） | ✅ |
| S-5 | `idor_clientCannotAccessOtherSecuritySnapshot` | A 的 token 查 B 的持仓 | 返回 ERR_018 | ✅ |
| S-6 | `idor_clientCannotChangeOtherFundPassword` | A 的 token 改 B 的资金密码 | 返回 ERR_018；B 的密码仍可登录（反向双重验证） | ✅ |
| S-7 | `tampered_clientTokenIsRejected` | 自造一个 64 hex 字符的伪 token | 返回 ERR_018 | ✅ |
| S-8 | `clientTokenHasSufficientEntropy` | 检查 token 形态 | 64 字符 hex（32 字节随机数），两次签发不重复 | ✅ |
| S-9 | `deactivatedStaffTokenIsImmediatelyRevoked` | staff02 登录后被 staff01 停用，再用旧 token 调内部接口 | 立即返回 ERR_018（`StaffServiceImpl#deactivateStaff` 调用了 `invalidateByStaffId`） | ✅ |
| S-10 | `passwordIsStoredAsHash` | 直接读 H2 中的 `trade_password` / `withdraw_password` | 长度为 64 hex 字符；与 `PasswordUtil.hash(明文)` 完全一致；绝非明文 | ✅ |
| S-11 | `wrongWithdrawPasswordDoesNotLeakBalance` | 错误取款密码 | 返回 ERR_004；响应里**不**回显 `available_balance` 字段 | ✅ |

## 5. 漏洞固化测试（6 项，全部通过 = 缺陷确认存在）

下列测试 **通过即代表系统当前确实存在该弱点**；将来修复后断言会失败，作为回归提醒。

### V1 — 交易回调接口完全无认证（高危）

- **测试方法**：`vuln_unauthenticatedTradeCallbackCanMutateBalances`
- **重现**：不带任何 token / header，直接 `POST /api/external/trade/fund-balance`，给受害者账户加 99999 元（`txn_type=卖出回款`）
- **结果**：返回 `code=0`，余额被实际改写
- **影响**：任何能访问 8080 端口的人都可以任意改资金余额和持仓；同理 `/api/external/trade/security-holding`
- **代码位置**：`ExternalTradeController` 没有任何 `@RequestHeader` 校验，README 也确认"当前未做独立系统签名"
- **建议修复**：在桥接层增加 HMAC 签名或独立 system token，并在 controller 层校验

### V2 — 登录错误信息可用于用户枚举（中危）

- **测试方法**：`vuln_loginErrorAllowsAccountEnumeration`
- **重现**：账户不存在 → `code=1010` (`ERR_010`)；账户存在但密码错 → `code=1004` (`ERR_004`)
- **影响**：攻击者可以遍历 `FA20260621000000`~`FA20260621999999` 区间枚举出有效资金账户号，再做撞库
- **代码位置**：`FundAccountServiceImpl#clientLoginAuth` 第 402-411 行
- **建议修复**：统一登录失败均返回 `ERR_004 "资金账户号或交易密码错误"`

### V3 — 任意 staff token 即可调用 `/api/admin` 系列管理员接口（高危）

- **测试方法**：`vuln_anyStaffTokenCanCallAdminEndpoints`
- **重现**：普通柜员登录后，用其 token 调用 `POST /api/admin/fund/settle-annual-interest`，成功
- **影响**：普通柜员可以执行年化结息、强制销户（`/api/admin/security/force-close`）、冻结/解冻账户、查全量审计日志（`/api/admin/audit/operation-logs`）
- **代码位置**：`AdminController` / `AuditController` 仅做 `staffAuthTokenService.requireAccess()`，没有 role/permission 校验。README 已声明 "不做 RBAC / permission 体系"
- **建议修复**：至少在 staff 表加 `role` 字段，并在 admin 接口中做角色校验

### V4 — 列表接口批量泄露 PII（高危）

- **测试方法**：`vuln_listAllAccountsLeaksPii`
- **重现**：任意 staff token 调用 `GET /api/internal/fund/accounts/list`，返回所有账户的姓名 + **完整 18 位身份证号**
- **影响**：一旦任一柜员账户被攻破或滥用，整个投资者库的 PII 一次性外泄
- **代码位置**：`FundAccountServiceImpl#listAllFundAccounts` 第 783 行；`SecurityAccountServiceImpl#listAllSecurityAccounts` 第 592 行
- **建议修复**：身份证号脱敏（如 `330101********0031`），分页 + 限速，并接 RBAC

### V5 — `auth_token` 通过 URL query 传递（中危）

- **测试方法**：`vuln_authTokenIsCarriedInQueryString`
- **重现**：`GET /api/external/fund/snapshot?fund_acc_no=...&auth_token=...` 是接口契约
- **影响**：token 会被记录到 Web 服务器 access log、上游代理日志、浏览器 Referer 头泄露给第三方
- **代码位置**：`ExternalFundController#getFundSnapshot` / `ExternalSecurityController#getSecuritySnapshot`
- **建议修复**：与 staff 端一致，改为通过 Header `auth_token` 或 `Authorization: Bearer <token>` 传递

### V6 — 缺失必填 header 时返回 500 + 内部异常信息（低-中危）

- **测试方法**：`vuln_missingStaffAuthTokenReturnsServerError`
- **重现**：内部接口不带 `X-Staff-Auth-Token`，返回 HTTP 500，body 含 "Required request header 'X-Staff-Auth-Token' for method parameter type String is not present" 等 Spring 内部异常文本
- **影响**：异常分类错误（应当是 401/400），且向客户端回显框架内部信息；同源问题影响 `GlobalExceptionHandler#handleUnknown`
- **代码位置**：`GlobalExceptionHandler.java` 未覆盖 `MissingRequestHeaderException`，被兜底的 `handleUnknown` 拼接 `ex.getMessage()`
- **建议修复**：在 `GlobalExceptionHandler` 中显式处理 `MissingRequestHeaderException`，映射为 ERR_018 + 401

## 6. 其他静态审计发现（未写成测试）

| 编号 | 风险 | 说明 |
|---|---|---|
| W1 | 密码哈希算法弱 | `PasswordUtil` 使用 **SHA-256 + 无 salt**，抗彩虹表能力差；金融系统建议改为 BCrypt / PBKDF2 / Argon2 + per-user salt |
| W2 | 数据库默认凭证 | `ServiceConfig.java` 默认 `username=root` / `password=root`，URL 含 `useSSL=false`；若部署时漏配 `ACCOUNT_DB_USERNAME` / `ACCOUNT_DB_PASSWORD` 环境变量，会自动用 root |
| W3 | 异常信息回显 | `GlobalExceptionHandler#handleUnknown` 直接返回 `"系统内部错误: " + ex.getMessage()`，与 V6 同源；建议线上环境改为统一错误码 + traceId，明细只写日志 |
| W4 | CORS 配置缺失 | grep 全代码库未发现任何 `CorsRegistry`/`@CrossOrigin`/`addCorsMappings` 配置，与 README 中 "已配置 CORS" 描述不符；要么未生效（前端无法跨域），要么被某个未追踪到的地方全开 |
| W5 | 缺少登录限速 | `StaffServiceImpl#staffLogin` 与 `FundAccountServiceImpl#clientLoginAuth` 都未做失败计数和退避，存在暴力破解面 |
| W6 | 黑名单服务异常被静默吞掉 | `FundAccountServiceImpl#createFundAccount` 第 78-80 行将黑名单校验异常包成 `log.warn` 后继续放行；攻击者只要把黑名单服务拒服即可绕过黑名单校验 |

## 7. 测试执行结果

```text
[INFO] Tests run: 17, Failures: 0, Errors: 0, Skipped: 0
[INFO] BUILD SUCCESS
```

完整 surefire 报告：
- `target/surefire-reports/TEST-account.integration.SecurityHardeningTest.xml`
- `target/surefire-reports/account.integration.SecurityHardeningTest.txt`

全量测试：

```text
[WARNING] Tests run: 67, Failures: 0, Errors: 0, Skipped: 8
[INFO] BUILD SUCCESS
```

跳过的 8 个为真实 MySQL 集成测试（需 `-Daccount.test.mysql.username=...` 提供凭证）。

## 8. 结论与优先级建议

**P0（必须在联调上线前修复）**

1. V1 — 给交易回调接口加签名验证
2. V3 — 给 admin 接口加角色权限
3. V4 — 列表接口对身份证号脱敏并加分页/限速

**P1（联调前修复）**

4. V2 — 统一登录错误信息
5. V5 — `auth_token` 改走 Header
6. W1 — 升级密码哈希算法（同时给迁移路径）
7. W2 — 移除默认凭证或在启动时强制要求环境变量

**P2（后续迭代）**

8. V6 / W3 — `GlobalExceptionHandler` 增补对 `MissingRequestHeaderException` 等的处理；统一错误响应模板
9. W4 — 修复或显式声明 CORS 策略
10. W5 — 登录失败限速
11. W6 — 黑名单依赖失败时改为业务失败（fail-closed），而非静默放行

---

## 附：本次涉及的代码改动

| 文件 | 类型 | 说明 |
|---|---|---|
| `src/main/java/account/service/api/StaffService.java` | 修复编译 | 补 `ChangeStaffPasswordRequest` import |
| `src/main/java/account/service/StaffServiceImpl.java` | 修复编译 | 实现 `changePassword`（哈希存储 + 操作日志） |
| `src/test/java/account/integration/SecurityHardeningTest.java` | 新增 | 17 个安全测试 |
| `安全测试报告.md` | 新增 | 本文件 |
